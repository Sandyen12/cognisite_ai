import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { AuthProvider } from '@/contexts/AuthContext'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'CogniSite AI - AI-Powered Website Content Generation',
  description: 'Analyze any website and generate high-quality, context-aware content with AI-powered conversations.',
  keywords: 'AI, website analysis, content generation, copywriting, marketing',
  authors: [{ name: 'CogniSite AI' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  )
}